# Separate service accounts per workload — principle of least privilege
apiVersion: v1
kind: ServiceAccount
metadata:
  name: backend-sa
  labels:
    app: backend
automountServiceAccountToken: false  # Don't mount token unless needed
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: frontend-sa
  labels:
    app: frontend
automountServiceAccountToken: false
