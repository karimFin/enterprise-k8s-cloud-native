# ⚠️ WARNING: In production, use SealedSecrets or External Secrets Operator!
# This file is here for demonstration only. NEVER commit real secrets to Git.
apiVersion: v1
kind: Secret
metadata:
  name: db-credentials
type: Opaque
stringData:
  DB_HOST: "postgres"
  DB_USER: "myapp"
  DB_PASSWORD: "CHANGE_ME_USE_SEALED_SECRETS"
  LLM_API_KEY: ""
  # Full connection URL for convenience
  DATABASE_URL: "postgres://myapp:CHANGE_ME_USE_SEALED_SECRETS@postgres:5432/myapp"
